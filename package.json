{
  "name": "project-init",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "project:init": "bun run ./setup/init.ts",
    "back:debug": "cd ./back && bun run debug",
    "front:dev": "cd ./front && bun run dev"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.6",
    "@types/bun": "latest",
    "dotenv": "^17.3.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "execa": "^9.6.1"
  }
}
